============
Installation
============

Simply run::

    pip install keybase

NB: Actually, this is currently a lie. The `keybase` API hasn't be pushed to PyPI yet. Soon though. For now you need to download, build and install the package from the github-based source.